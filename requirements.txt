# Install torch first, matched to your CUDA driver, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
torch>=2.6
transformers>=4.56
trl>=0.23
peft>=0.14
accelerate>=1.0
datasets>=3.0
bitsandbytes>=0.45
evaluate>=0.4
rouge_score
tensorboard
tqdm
jinja2>=3.1.0
